#include <iostream>
using namespace std; 
     int main(){
     int total;
       
        cout<<"berapakah Total Belanjaan Rp:" ;
        cin>>total;
        if(total >= 52000000){
            cout<<"--------------------------------"<<endl;
            cout<<"Test Passed, Total is, Rp 52.000.000"<<endl;
         }else{
            cout<<"--------------------------------"<<endl;
            cout<<"Test Failed, Total is must, Rp 52.000.000"<<endl;
         }
     }
